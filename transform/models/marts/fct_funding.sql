select
    application_id,
    applicant_id,
    decision_at,
    vehicle_selected_at,
    contract_started_at,
    contracted_at,
    funding_started_at,
    funded_at,
    funding_status,
    funding_timestamp,
    funding_duration_hours,
    hours_decision_to_funded,
    stipulation_flag,
    reached_vehicle_selected,
    is_contracted,
    is_funded,
    risk_band,
    underwriting_path,
    experiment_variant,
    device_type,
    browser,
    device_browser,
    acquisition_channel,
    returning_user,
    age_band,
    income_band,
    employment_type,
    state,
    started_week,
    started_month
from {{ ref('fct_applications') }}
where is_approved
